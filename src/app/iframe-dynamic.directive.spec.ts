import { IframeDynamicDirective } from './iframe-dynamic.directive';

describe('IframeDynamicDirective', () => {
  it('should create an instance', () => {
    const directive = new IframeDynamicDirective();
    expect(directive).toBeTruthy();
  });
});
